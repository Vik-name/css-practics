{{#with footer}}
<footer class="footer">
    <div class="list_page_link">
        {{#each pages}}
        <a href="{{path}}" class="link_pages">{{name}}</a>
        {{/each}}
    </div>

    <div>
        <span class="footer_copyright">{{copyright}}</span>
    </div>
</footer>
{{/with}}